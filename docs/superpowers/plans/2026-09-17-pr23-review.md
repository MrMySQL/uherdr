# PR #23 review follow-up plan

> **For agentic workers:** Use the debugging and code-review skills; independent import and UI investigations run with the dispatching-parallel-agents skill.

**Goal:** Address the nine unresolved review findings on PR #23 and verify the resulting branch and remote checks.

**Architecture:** Preserve normalized, immutable appearance snapshots. Reject native copying when import normalization would lose unsupported sidebar settings; keep ordinary imports tolerant of unknown TOML values. Fix rendering and asynchronous test behavior at their existing boundaries.

**Tech Stack:** Swift, SwiftUI, TOMLKit 0.5.0, shell test runners.

**Spec:** `docs/superpowers/specs/2026-09-16-coloring-design.md` and PR #23's nine inline review findings.

## Constraints

Keep macOS 14, Herdr 0.9.0, existing terminal identities, read-only imports, and device-local preferences. Do not change vendor code or unrelated worktrees.

## Tasks

- [x] Import agent: reproduce unknown nonfinite TOML failure in core tests; replace the JSON encoding bridge without accepting invalid supported fields. Verify `swift run HerdrCoreTests`.
- [x] Import agent: test unsupported-field copying, including restart; block copy with a diagnostic and preserve saved native state. Verify `AppearanceStoreTests`.
- [x] Sidebar agent: preserve complete accessibility content, suppress configured empty rows, bound UI row spacing, and clear invalid conditional rules when changing token kinds.
- [x] Sidebar agent: replace correctness-related fixed sleeps with polling for popup state and exact clipboard selection in mounted tests.
- [x] Root: stat paths before opening and retain descriptor validation; verify device, symlink, FIFO, directory, size, and last-good behavior in appearance tests.
- [x] Root: make pane-drag build configuration match its linker configuration, including release testability and separate objects. Execute the release runner.
- [x] Root: fix quoted SwiftPM object records in `scripts/app-test-link.sh`; reproduce with a fixture and verify both native and Xcode layouts.
- [x] Root: inspect the integrated diff, run appearance and aggregate suites and whitespace validation, and obtain independent review. All passed.
- Commit and push the verified fixes.
- Remote follow-up: verify the pushed commit's checks and review threads, resolve addressed threads, and monitor for new review feedback until ready.
